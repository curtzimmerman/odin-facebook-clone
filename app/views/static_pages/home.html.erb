<div class="container">
	<% if user_signed_in? %>
		<div class="post-form">
			<%= render 'posts/post_form' %>
		</div>
		<h1>News Feed</h1>	
		<div class="feed">
			<%= render @feed %>
		</div>
	<% else %>
		<h1>Odin Facebook Clone</h1>
	<% end %>
</div>